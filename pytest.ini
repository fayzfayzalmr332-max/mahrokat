[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
# تحذير PTB عن per_message=False مقصود وموثّق في build_application (bot.py):
# المحادثة تُدار per_chat/per_user والأزرار تعمل بنمط الدردشة الخاصة.
filterwarnings =
    ignore:If 'per_message=False':UserWarning